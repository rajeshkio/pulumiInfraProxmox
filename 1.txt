kubectl exec -ti {prometheus_exporter_pod_id} -n neuvector -- sh
cd /usr/local/bin/
mv nv_exporter.py nv_exporter.py.orig
exit
// now copy the private nv_exporter.py into prometheus_exporter pod by:
kubectl cp nv_exporter.py neuvector/{prometheus_exporter_pod_id}:/usr/local/bin/nv_exporter.py

kubectl exec -ti {prometheus_exporter_pod_id} -n neuvector -- sh
ls -l /usr/local/bin/
# there should be nv_exporter.py.orig & nv_exporter.py listed

=> Now see whether the issue is fixed
